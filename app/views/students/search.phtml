<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["students/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["students/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>StudentID</th>
            <th>LastName</th>
            <th>FirstName</th>
            <th>Address</th>
            <th>City</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $student): ?>
            <tr>
                <td><?php echo $student->StudentID ?></td>
            <td><?php echo $student->LastName ?></td>
            <td><?php echo $student->FirstName ?></td>
            <td><?php echo $student->Address ?></td>
            <td><?php echo $student->City ?></td>

                <td><?php echo $this->tag->linkTo(["students/edit/" . $student->StudentID, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["students/delete/" . $student->StudentID, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("students/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("students/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("students/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("students/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
